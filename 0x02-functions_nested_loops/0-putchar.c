#include <stdio.h>
#include <string.h>

/**
 * main - prints a char by char with a loop
 * Return: 0
 */
int main()
{
	int len;
	char quote[9];
	int index;

	index = 0;
	quote = "_putchar"
	len = strlen(quote);
	while (i < len)
		putchar(quote[i]);
	return (0);
}
