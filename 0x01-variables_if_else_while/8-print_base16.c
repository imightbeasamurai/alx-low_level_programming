#include <stdio.h>

/**
 * main - print base 16
 * Return: 0
 */
int main(void)
{
	int n;
	int a;

	for (n = 48; n < 58; n++)
		putchar(n);
	for (ch = 'a'; a <= 'f'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
